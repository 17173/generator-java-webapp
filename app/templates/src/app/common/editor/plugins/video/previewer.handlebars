{{#if src}}
<embed
   src="{{src}}"
   allowFullScreen="true"
   quality="high"
   width="{{width}}"
   height="{{height}}"
   align="middle"
   allowScriptAccess="always"
   type="application/x-shockwave-flash"
  />
{{else}}
预览区
{{/if}}